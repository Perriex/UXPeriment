/// auto add lazy loading to all images on current page
window.addEventListener("DOMContentLoaded",function(){let e=document.querySelectorAll("img, iframe");e.forEach(e=>{e.setAttribute("loading","lazy")})});

// todo
// check internet speed and cancel large media

// todo
// check no-use UI and delete them
